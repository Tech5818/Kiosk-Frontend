import styled from "styled-components";
import imgPath from "@shared/assets/MacIcon.png";

export const MacIcon = () => {
  return (
    <>
      <Icon src={imgPath} />
    </>
  );
};

const Icon = styled.img``;
